# Anthropic API key — get one at platform.anthropic.com
# This is NOT your Claude Pro/Max subscription key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: max generations per IP per rolling 24 hours (default 20)
RATE_LIMIT_PER_DAY=20

# Optional: max daily API spend in USD before friendly fallback (default $5)
DAILY_COST_CAP=5
